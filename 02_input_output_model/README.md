# Input / Output Model Exercises

Investigation of the Linux input/output model and system calls

## List of Projects

- **m_tee.c** - basic implementation of a **tee** command. Reads the STDIN, writes to STDOUT and a file provided as an argument, flag **-a** (append) implemented
